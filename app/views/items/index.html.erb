<%= render "header" %>

<h2>Marketplace Items</h2>

<p>
  <%= link_to "New Item", new_item_path if authenticated? %>
</p>

<% if @items.any? %>
  <% @items.each do |item| %>
    <%= render item, link_title: true %>
  <% end %>
<% else %>
  <p>No items yet.</p>
<% end %>
