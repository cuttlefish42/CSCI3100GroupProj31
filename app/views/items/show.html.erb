<div class="container mx-auto p-4 flex flex-col md:flex-row gap-8">
  <!-- Left Column -->
  <%= render "item_details" %>

  <!-- Right Column -->
  <%= render "item_sidebar" %>
</div>

<div class="container mx-auto p-4">
<%= render "item_offers" %>

</div>
