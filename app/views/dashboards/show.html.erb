<div class="container mx-auto p-4">
  <h1>Dashboard</h1>

  <h2>Item Analytics</h2>
  <% if @my_items.any? %>
    <div class="bg-base-100 rounded-box shadow p-4 mb-6">
      <div role="tablist" class="tabs tabs-boxed mb-4">
        <% @my_items.each_with_index do |item, i| %>
          <input type="radio" name="analytics_tab" role="tab" class="tab"
                 aria-label="<%= item.title %>" <%= "checked" if i == 0 %> />
          <div role="tabpanel" class="tab-content pt-4">
            <div class="h-64">
              <canvas data-controller="item-analytics-chart"
                      data-item-analytics-chart-url-value="<%= analytics_item_path(item) %>">
              </canvas>
            </div>
          </div>
        <% end %>
      </div>
    </div>
  <% else %>
    <p class="text-base-content/50 mb-6">No items listed yet.</p>
  <% end %>

  <h2>Pending Transactions</h2>
  <% if @pending_transactions.any? %>
    <%= render "dashboards/offers_table",
      offers: @pending_transactions,
      sort: @sent_sort,
      dir: @sent_dir,
      sort_prefix: "sent",
      second_header: "Buyer",
      second_key: "seller",
      price_header: "Offer",
      row_partial: "dashboards/pending_action_row" %>
  <% else %>
    <p class="text-base-content/50">no pending actions</p>
  <% end %>
  <%= turbo_stream_from "user_#{Current.user.id}_offer_updates" %>

  <h2>Offers I've Made</h2>
  <% if @sent_offers.any? %>
    <div class="overflow-x-auto bg-base-100 rounded-box shadow">
      <table class="table-zebra w-full text-center">
    <thead>
      <tr>
        <th><%= sortable_header("Item", "item", @sent_sort, @sent_dir, sort_param: "sent_sort", dir_param: "sent_dir") %></th>
        <th><%= sortable_header("Seller", "seller", @sent_sort, @sent_dir, sort_param: "sent_sort", dir_param: "sent_dir") %></th>
        <th><%= sortable_header("My Offer", "price", @sent_sort, @sent_dir, sort_param: "sent_sort", dir_param: "sent_dir") %></th>
        <th>Message</th>
        <th><%= sortable_header("Status", "status", @sent_sort, @sent_dir, sort_param: "sent_sort", dir_param: "sent_dir") %></th>
        <th><%= sortable_header("Date", "date", @sent_sort, @sent_dir, sort_param: "sent_sort", dir_param: "sent_dir") %></th>
        <th>Actions</th>
      </tr>
    </thead>
    <tbody>
      <% @sent_offers.each do |offer| %>
        <%= render "dashboards/sent_offer_row", offer: offer %>
      <% end %>
    </tbody>
  </table>
  </div>
<% else %>
  <p class="text-base-content/50">You haven't made any offers yet.</p>
<% end %>

<h2>Offers I've Received</h2>
<% if @received_offers.any? %>
  <div class="overflow-x-auto bg-base-100 rounded-box shadow">
    <table class="table-zebra w-full text-center">
    <thead>
      <tr>
        <th><%= sortable_header("Item", "item", @recv_sort, @recv_dir, sort_param: "recv_sort", dir_param: "recv_dir") %></th>
        <th><%= sortable_header("Buyer", "buyer", @recv_sort, @recv_dir, sort_param: "recv_sort", dir_param: "recv_dir") %></th>
        <th><%= sortable_header("Amount", "price", @recv_sort, @recv_dir, sort_param: "recv_sort", dir_param: "recv_dir") %></th>
        <th>Message</th>
        <th><%= sortable_header("Status", "status", @recv_sort, @recv_dir, sort_param: "recv_sort", dir_param: "recv_dir") %></th>
        <th><%= sortable_header("Date", "date", @recv_sort, @recv_dir, sort_param: "recv_sort", dir_param: "recv_dir") %></th>
        <th>Actions</th>
      </tr>
    </thead>
    <tbody>
      <% @received_offers.each do |offer| %>
        <%= render "dashboards/received_offer_row", offer: offer %>
      <% end %>
    </tbody>
  </table>
  </div>
<% else %>
  <p class="text-base-content/50">You haven't received any offers yet.</p>
<% end %>
</div>
